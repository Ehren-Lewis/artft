export const metadata = {
    title: 'Artist Login | ArtFT',
    description: 'Temp',
}

export default function sellLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}