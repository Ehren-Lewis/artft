export const metadata = {
    title: 'Artists | ArtFT',
    description: 'Temp',
}

export default function sellLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}