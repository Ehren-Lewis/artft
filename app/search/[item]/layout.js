export const metadata = {
    title: 'Search | ArtFT',
    description: 'Temp',
}

export default function itemLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}