export const metadata = {
    title: 'Collections | ArtFT',
    description: 'Temp',
}

export default function sellLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}