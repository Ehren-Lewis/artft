export const metadata = {
    title: 'About | ArtFT',
    description: 'Temp',
}

export default function aboutLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}