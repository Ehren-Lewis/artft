export const metadata = {
    title: 'Artist Sign UP | ArtFT',
    description: 'Temp',
}

export default function sellLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}