export const metadata = {
    title: 'Artist Home | ArtFT',
    description: 'Temp',
}

export default function sellLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}